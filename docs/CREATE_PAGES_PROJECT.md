# Створення Cloudflare Pages проекту вручну

Якщо Wrangler не може створити проект автоматично, створіть його вручну.

## 🎯 Коли потрібно

Якщо бачите помилку:
```
Project not found. The specified project name does not match any of your existing projects.
```

## ✅ Покрокова інструкція

### Крок 1: Відкрийте Cloudflare Dashboard

1. Йдіть: https://dash.cloudflare.com
2. Увійдіть у свій акаунт

### Крок 2: Перейдіть до Pages

1. У лівому меню знайдіть **Pages**
2. Клікніть на **Pages**

Або прямо: https://dash.cloudflare.com/pages

### Крок 3: Створіть новий проект

1. Клікніть кнопку **Create a project** (великий синій +)

### Крок 4: Виберіть Direct Upload

1. Побачите два варіанти:
   - **Connect to Git** (ліворуч)
   - **Direct Upload** (праворуч)
2. Клікніть **Direct Upload** (праворуч зверху)

### Крок 5: Вкажіть назву проекту

**ДУЖЕ ВАЖЛИВО**: Назва має **ТОЧНО** співпадати з вашим GitHub Secret `CLOUDFLARE_PAGES_PROJECT`

1. У полі **Project name** введіть назву
   - Наприклад: `mova-smartlink-admin`
   - Має бути **lowercase** (маленькі літери)
   - Можна використовувати **дефіси** `-`
   - **БЕЗ пробілів, підкреслень, великих літер**

2. **Як дізнатися точну назву з GitHub Secret**:
   ```
   GitHub Repository
     → Settings
     → Secrets and variables
     → Actions
     → CLOUDFLARE_PAGES_PROJECT
     → (скопіюйте значення)
   ```

### Крок 6: Створіть проект

1. Клікніть **Create project**
2. Побачите екран завантаження
3. Проект буде створено

### Крок 7: (Опціонально) Завантажте тестові файли

Cloudflare може попросити завантажити файли для першого деплою:

1. Можете **пропустити** це (Skip)
2. Або завантажити будь-який HTML файл

**Не хвилюйтеся**: GitHub Actions перезапише це при наступному деплої.

### Крок 8: Запустіть GitHub Actions деплой

Тепер проект існує, можна деплоїти:

```
GitHub → Actions → Deploy Pages → Run workflow → Run workflow
```

✅ **Має запрацювати!**

---

## 📋 Перевірка проекту

Після створення перевірте:

1. **Cloudflare Dashboard** → **Pages**
2. Побачите ваш проект у списку
3. Назва проекту має співпадати з `CLOUDFLARE_PAGES_PROJECT`

---

## 🔍 Troubleshooting

### Помилка "Project name already exists"

**Проблема**: Проект з такою назвою вже існує.

**Рішення**:
- Якщо це ваш проект - використовуйте його (нічого не створюйте)
- Якщо це чужий проект - виберіть іншу назву
- Оновіть `CLOUDFLARE_PAGES_PROJECT` secret у GitHub

### Помилка "Invalid project name"

**Проблема**: Назва проекту має невалідні символи.

**Вимоги до назви**:
- ✅ Lowercase літери (a-z)
- ✅ Цифри (0-9)
- ✅ Дефіси (-)
- ❌ БЕЗ великих літер
- ❌ БЕЗ пробілів
- ❌ БЕЗ підкреслень (_)
- ❌ БЕЗ спецсимволів

**Правильно**: `mova-smartlink-admin`, `my-app-123`  
**Неправильно**: `Mova_Smartlink`, `my app`, `MY-APP`

### Проект створено, але деплой все ще падає

**Перевірте**:

1. **Назви співпадають**:
   ```
   Cloudflare Project Name = CLOUDFLARE_PAGES_PROJECT Secret
   ```

2. **API Token має права**:
   - Йдіть: https://dash.cloudflare.com/profile/api-tokens
   - Перевірте, що токен має **Cloudflare Pages Edit** permission

3. **Account ID правильний**:
   - Має бути з того самого акаунту, де створений проект
   - Перевірте в URL: `dash.cloudflare.com/{ACCOUNT_ID}/pages`

---

## 🎬 Відео-інструкція (текстова)

```
1. dash.cloudflare.com
   ↓
2. Ліве меню → Pages
   ↓
3. Create a project
   ↓
4. Direct Upload (праворуч зверху)
   ↓
5. Project name: mova-smartlink-admin
   ↓
6. Create project
   ↓
7. GitHub Actions → Run workflow
   ↓
8. ✅ Деплой успішний!
```

---

## 📊 Після успішного створення

Ваш проект буде доступний:
- 🌐 Production: `https://mova-smartlink-admin.pages.dev`
- 📊 Dashboard: `https://dash.cloudflare.com/pages/view/mova-smartlink-admin`

---

## 🔄 Альтернативний метод: Git Integration

Якщо хочете автоматичні деплої через Git:

1. **Create a project** → **Connect to Git**
2. Виберіть GitHub
3. Авторизуйте Cloudflare
4. Виберіть репозиторій `mova_smartlink`
5. **Build settings**:
   - Build command: `npm run build:pages`
   - Build output directory: `packages/spa-admin/dist`
   - Root directory: `/`
6. **Environment variables**: (none needed)
7. Save and Deploy

**Переваги Git Integration**:
- Автоматичний деплой на кожен push
- Preview deployments для PR
- Не потрібен GitHub Actions для деплою

**Недоліки**:
- Менше контролю
- Потрібна авторизація GitHub

---

**Рекомендую**: Створіть проект вручну через Direct Upload (швидше і простіше).

**Наступний крок**: Запустіть GitHub Actions workflow для деплою.

